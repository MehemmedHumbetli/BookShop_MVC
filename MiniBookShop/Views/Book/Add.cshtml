@model BookViewModel


<div class="container mt-5 d-flex justify-content-center">
    <div class="card book-card p-4">
        <h2 class="text-center mb-4">📚 Add New Book</h2>
        <form asp-action="Add" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label>Book Name</label>
                <input asp-for="Book.Name" class="form-control" placeholder="Enter book name">
            </div>

            <div class="form-group">
                <label>Author</label>
                <input asp-for="Book.Author" class="form-control" placeholder="Enter author's name">
            </div>

            <div class="form-group">
                <label >Genre</label>
                <input asp-for="Book.Genre" class="form-control" placeholder="Enter genre">
            </div>

            <div class="form-group">
                <label>Pages</label>
                <input asp-for="Book.Pages" type="number" class="form-control" placeholder="Enter page count">
            </div>

            <div class="form-group">
                <label>Read Count</label>
                <input asp-for="Book.ReadCount" type="number" class="form-control" placeholder="Enter read count">
            </div>

            <div class="form-group">
                <label>Description</label>
                <textarea asp-for="Book.Decription" class="form-control" rows="3" placeholder="Enter description"></textarea>
            </div>

            <div class="form-group">
                <label>Price ($)</label>
                <input asp-for="Book.Price" type="number" class="form-control" placeholder="Enter price">
            </div>

            <button type="submit" class="btn btn-primary btn-block add-btn">Add Book</button>
        </form>
    </div>
</div>

<style>
    body {
        background: linear-gradient(135deg, #1c1c1c, #3a3a3a, #5a5a5a);
        font-family: 'Poppins', sans-serif;
    }

    .book-card {
        color: whitesmoke;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border-radius: 15px;
        width: 500px;
        box-shadow: 0px 8px 30px rgba(255, 255, 255, 0.2);
    }

        .book-card h2 {
            font-weight: 700;
        }

    .form-control {
        background: rgba(255, 255, 255, 0.2);
        border: none;
        border-radius: 8px;
        padding: 12px;
        color: white;
    }

        .form-control::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.3);
            color: white;
            outline: none;
            box-shadow: 0px 0px 10px rgba(255, 204, 102, 0.8);
        }

    .add-btn {
        background: linear-gradient(135deg, #ffcc66, #ffb347);
        color: white;
        font-size: 18px;
        font-weight: bold;
        padding: 12px;
        border-radius: 10px;
        transition: all 0.3s ease-in-out;
        box-shadow: 0px 5px 15px rgba(255, 204, 102, 0.5);
        margin-top: 10px;
    }

        .add-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0px 8px 20px rgba(255, 204, 102, 0.8);
        }
</style>
